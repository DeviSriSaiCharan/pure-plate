rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Demo Mode Rules: Allow public access to all collections for Capstone Showcase
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
